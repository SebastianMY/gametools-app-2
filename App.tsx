/**
 * Entry point for the Expo app.
 * Imports the main App component from src/App.tsx.
 * react-native-gesture-handler must be imported first.
 */
import 'react-native-gesture-handler';

export { default } from './src/App';
